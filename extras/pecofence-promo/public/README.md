# Generated media

This directory holds local demo recordings, stills and generated soundtracks.
These assets are intentionally excluded from the public source export.

Use the capture scripts and `features:prepare` / `make-assets.mjs` commands described
in the parent README to regenerate media before rendering the promotional videos.
